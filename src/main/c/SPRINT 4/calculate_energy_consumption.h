#ifndef CALCULATE_ENERGY_CONSUMPTION_H
#define CALCULATE_ENERGY_CONSUMPTION_H
#include "create_container_structure.h"
#include "energy_needed.h"
bool calculate_energy_consumption(Container* container_array, float generatorCapacity, int count);
#endif
